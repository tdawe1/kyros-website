---
// Header component for the website
---

<header>
  <nav>
    <div class="logo">
      <h1>Bridge & Signal</h1>
    </div>
    <ul class="nav-links">
      <li><a href="#process">Process</a></li>
      <li><a href="#case-studies">Case Studies</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
  </nav>
  
  <div class="section-container">
    <div class="hero-grid">
      <div class="hero-content">
        <h2>Connecting Ideas and People</h2>
        <p>We bridge the gap between vision and execution, creating meaningful connections that drive success.</p>
        <a href="#process" class="cta-button">Learn More</a>
      </div>
      <div class="hero-visual">
        <!-- Placeholder for banner image -->
      </div>
    </div>
  </div>
</header>

<!-- Floating Menu Icon -->
<div class="floating-menu-icon" id="floatingMenuIcon">
  <div class="menu-icon">
    <span></span>
    <span></span>
    <span></span>
  </div>
</div>

<!-- Floating Menu Overlay -->
<div class="floating-menu-overlay" id="floatingMenuOverlay">
  <div class="floating-menu-content">
    <div class="menu-header">
      <h3>Navigation</h3>
      <button class="close-menu" id="closeMenu">×</button>
    </div>
    <nav class="floating-nav">
      <ul>
        <li><a href="#process">Our Process</a></li>
        <li><a href="#case-studies">Case Studies</a></li>
        <li><a href="#why-us">Why Us</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
    <div class="menu-footer">
      <p>Ready to get started?</p>
      <a href="#contact" class="menu-cta">Let's Talk</a>
    </div>
  </div>
</div>

<style>
  header {
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: left;
    padding: 2rem 1rem;
    background: linear-gradient(135deg, #F8F9FA 0%, #ffffff 100%);
  }

  nav {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 2rem;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
  }

  .logo h1 {
    font-size: 64px;
    font-weight: 700;
    color: var(--text-dark);
    margin: 0;
    text-align: left;
  }

  .nav-links {
    display: flex;
    list-style: none;
    gap: 2rem;
    margin: 0;
    padding: 0;
  }

  .nav-links a {
    text-decoration: none;
    color: var(--text-dark);
    font-weight: 500;
    transition: color 0.3s ease;
  }

  .nav-links a:hover {
    color: var(--accent-blue);
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4rem;
    align-items: center;
    width: 100%;
  }

  .hero-content {
    max-width: 600px;
  }

  .hero-content h2 {
    font-size: 4rem;
    font-weight: 700;
    color: var(--text-dark);
    margin-bottom: 1.5rem;
    line-height: 1.2;
    text-align: left;
  }

  .hero-content p {
    font-size: 20px;
    color: var(--text-dark);
    margin-bottom: 2.5rem;
    line-height: 1.6;
    text-align: left;
  }

  .hero-visual {
    height: 400px;
    background: linear-gradient(135deg, var(--accent-blue) 0%, #0056b3 100%);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.2rem;
    font-weight: 500;
    opacity: 0.8;
  }

  .cta-button {
    display: inline-block;
    background-color: var(--accent-blue);
    color: white;
    padding: 1rem 2rem;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 123, 255, 0.3);
  }

  .cta-button:hover {
    background-color: #0056b3;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 123, 255, 0.4);
  }

  /* Floating Menu Icon */
  .floating-menu-icon {
    position: fixed;
    top: 2rem;
    right: 2rem;
    z-index: 1000;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    transform: translateY(-20px);
  }

  .floating-menu-icon.visible {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .menu-icon {
    width: 60px;
    height: 60px;
    background: var(--accent-blue);
    border-radius: 50%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    box-shadow: 0 4px 20px rgba(0, 123, 255, 0.3);
    transition: all 0.3s ease;
  }

  .menu-icon:hover {
    background: #0056b3;
    transform: scale(1.1);
    box-shadow: 0 6px 25px rgba(0, 123, 255, 0.4);
  }

  .menu-icon span {
    width: 24px;
    height: 3px;
    background: white;
    margin: 2px 0;
    border-radius: 2px;
    transition: all 0.3s ease;
  }

  /* Floating Menu Overlay */
  .floating-menu-overlay {
    position: fixed;
    top: 0;
    right: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    z-index: 999;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
  }

  .floating-menu-overlay.active {
    opacity: 1;
    visibility: visible;
  }

  .floating-menu-content {
    position: absolute;
    top: 0;
    right: 0;
    width: 33.333%;
    height: 100%;
    background: var(--text-dark);
    color: white;
    padding: 3rem 2rem;
    transform: translateX(100%);
    transition: transform 0.3s ease;
    overflow-y: auto;
  }

  .floating-menu-overlay.active .floating-menu-content {
    transform: translateX(0);
  }

  .menu-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
  }

  .menu-header h3 {
    font-size: 2rem;
    color: white;
    margin: 0;
  }

  .close-menu {
    background: none;
    border: none;
    color: white;
    font-size: 2.5rem;
    cursor: pointer;
    padding: 0;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    transition: background-color 0.3s ease;
  }

  .close-menu:hover {
    background: rgba(255, 255, 255, 0.1);
  }

  .floating-nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .floating-nav li {
    margin-bottom: 1.5rem;
  }

  .floating-nav a {
    color: white;
    text-decoration: none;
    font-size: 1.25rem;
    font-weight: 500;
    transition: color 0.3s ease;
    display: block;
    padding: 1rem 0;
  }

  .floating-nav a:hover {
    color: var(--accent-blue);
  }

  .menu-footer {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.2);
  }

  .menu-footer p {
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 1.5rem;
    font-size: 1.1rem;
  }

  .menu-cta {
    display: inline-block;
    background: var(--accent-blue);
    color: white;
    padding: 1rem 2rem;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: background-color 0.3s ease;
  }

  .menu-cta:hover {
    background: #0056b3;
  }

  @media (max-width: 768px) {
    header {
      min-height: 100vh;
      padding: 1rem;
    }

    nav {
      padding: 1rem;
      flex-direction: column;
      gap: 1rem;
    }

    .logo h1 {
      font-size: 2.5rem;
    }

    .nav-links {
      gap: 1rem;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-content h2 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .hero-content p {
      font-size: 1.1rem;
      margin-bottom: 2rem;
    }

    .hero-visual {
      height: 200px;
      order: -1;
    }

    .cta-button {
      padding: 0.875rem 1.75rem;
      font-size: 1rem;
    }

    .floating-menu-content {
      width: 100%;
    }

    .floating-menu-icon {
      top: 1rem;
      right: 1rem;
    }

    .menu-icon {
      width: 50px;
      height: 50px;
    }
  }
</style>

<script>
  // Floating menu functionality
  let lastScrollTop = 0;
  const floatingMenuIcon = document.getElementById('floatingMenuIcon');
  const floatingMenuOverlay = document.getElementById('floatingMenuOverlay');
  const closeMenu = document.getElementById('closeMenu');

  // Only proceed if all elements exist
  if (floatingMenuIcon && floatingMenuOverlay && closeMenu) {
    // Show/hide floating menu icon based on scroll
    window.addEventListener('scroll', () => {
      const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
      
      if (scrollTop > 100) {
        floatingMenuIcon!.classList.add('visible');
      } else {
        floatingMenuIcon!.classList.remove('visible');
      }
      
      lastScrollTop = scrollTop;
    });

    // Open menu
    floatingMenuIcon!.addEventListener('click', () => {
      floatingMenuOverlay!.classList.add('active');
      document.body.style.overflow = 'hidden';
    });

    // Close menu
    closeMenu!.addEventListener('click', () => {
      floatingMenuOverlay!.classList.remove('active');
      document.body.style.overflow = '';
    });

    // Close menu when clicking outside
    floatingMenuOverlay!.addEventListener('click', (e) => {
      if (e.target === floatingMenuOverlay) {
        floatingMenuOverlay!.classList.remove('active');
        document.body.style.overflow = '';
      }
    });

    // Close menu on escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && floatingMenuOverlay!.classList.contains('active')) {
        floatingMenuOverlay!.classList.remove('active');
        document.body.style.overflow = '';
      }
    });
  }
</script>
