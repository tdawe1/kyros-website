---
// Header component for the website
---

<header>
  <nav>
    <div class="logo">
      <h1>kyros</h1>
    </div>
    <ul class="nav-links">
      <li><a href="#process">Our Process</a></li>
      <li><a href="#case-studies">Case Studies</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="/discovery" class="nav-cta">Book A Call</a></li>
    </ul>
  </nav>
  
  <div class="section-container">
    <div class="hero-grid">
      <div class="hero-content">
        <h2>Automation without the bloat. ROI without the gamble.</h2>
      </div>
      <div class="hero-visual">
        <div class="hero-pattern">
          <div class="pattern-circle circle-1"></div>
          <div class="pattern-circle circle-2"></div>
          <div class="pattern-circle circle-3"></div>
          <div class="pattern-line line-1"></div>
          <div class="pattern-line line-2"></div>
        </div>
        <div class="hero-text">
          <span>Workflow</span>
          <span>Automation</span>
        </div>
      </div>
    </div>
  </div>
</header>

<style>
  header {
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: left;
    padding: 2.1rem 1.05rem;
    background: 
      linear-gradient(135deg, #F8F9FA 0%, #ffffff 100%),
      radial-gradient(circle at 25% 25%, rgba(0, 123, 255, 0.03) 0%, transparent 50%),
      radial-gradient(circle at 75% 75%, rgba(0, 86, 179, 0.03) 0%, transparent 50%),
      linear-gradient(45deg, transparent 49%, rgba(0, 123, 255, 0.02) 50%, transparent 51%);
    background-size: 100% 100%, 200px 200px, 200px 200px, 60px 60px;
    position: relative;
    overflow: hidden;
  }

  header::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      radial-gradient(ellipse at top, rgba(0, 123, 255, 0.05) 0%, transparent 70%),
      radial-gradient(ellipse at bottom, rgba(0, 86, 179, 0.05) 0%, transparent 70%);
    pointer-events: none;
  }

  nav {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 2.1rem 3.15rem;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    z-index: 10;
  }

  .logo h1 {
    font-size: 67.2px;
    font-weight: 700;
    color: var(--text-dark);
    margin: 0;
    text-align: left;
  }

  .nav-links {
    display: flex;
    list-style: none;
    gap: 2.625rem;
    margin: 0;
    padding: 0;
    align-items: center;
  }

  .nav-links a {
    text-decoration: none;
    color: var(--text-dark);
    font-weight: 500;
    font-size: 1.155rem;
    transition: all 0.3s ease;
    padding: 0.525rem 0;
  }

  .nav-links a:hover {
    color: var(--accent-blue);
    transform: translateY(-2.1px);
  }

  .nav-cta {
    background: linear-gradient(135deg, var(--accent-blue) 0%, var(--electric-blue-dark) 100%);
    color: white !important;
    padding: 0.7875rem 1.575rem !important;
    border-radius: 8.4px;
    font-weight: 600;
    box-shadow: 0 4.2px 15.75px rgba(0, 123, 255, 0.3);
    transition: all 0.3s ease;
  }

  .nav-cta:hover {
    transform: translateY(-3.15px) !important;
    box-shadow: 0 6.3px 21px rgba(0, 123, 255, 0.4);
    color: white !important;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4.2rem;
    align-items: center;
    width: 100%;
    position: relative;
    z-index: 5;
  }

  .hero-content {
    max-width: 630px;
  }

  .hero-content h2 {
    font-size: 4.2rem;
    font-weight: 700;
    color: var(--text-dark);
    margin: 0;
    line-height: 1.2;
    text-align: left;
  }

  .hero-visual {
    height: 420px;
    background: linear-gradient(135deg, var(--accent-blue) 0%, var(--electric-blue-dark) 100%);
    border-radius: 12.6px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.26rem;
    font-weight: 500;
    position: relative;
    overflow: hidden;
  }

  .hero-pattern {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
  }

  .pattern-circle {
    position: absolute;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    animation: float 6s ease-in-out infinite;
  }

  .circle-1 {
    width: 80px;
    height: 80px;
    top: 20%;
    left: 20%;
    animation-delay: 0s;
  }

  .circle-2 {
    width: 60px;
    height: 60px;
    top: 60%;
    right: 20%;
    animation-delay: 2s;
  }

  .circle-3 {
    width: 40px;
    height: 40px;
    bottom: 20%;
    left: 30%;
    animation-delay: 4s;
  }

  .pattern-line {
    position: absolute;
    background: rgba(255, 255, 255, 0.1);
    animation: flow 8s ease-in-out infinite;
  }

  .line-1 {
    width: 2px;
    height: 120px;
    top: 10%;
    right: 30%;
    animation-delay: 1s;
  }

  .line-2 {
    width: 120px;
    height: 2px;
    bottom: 30%;
    left: 10%;
    animation-delay: 3s;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    z-index: 2;
  }

  .hero-text span {
    font-size: 1.5rem;
    font-weight: 600;
    opacity: 0.9;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }

  @keyframes flow {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 0.7; }
  }

  @media (max-width: 768px) {
    header {
      min-height: 100vh;
      padding: 1.05rem;
    }

    nav {
      padding: 1.05rem;
      flex-direction: column;
      gap: 1.05rem;
    }

    .logo h1 {
      font-size: 2.625rem;
    }

    .nav-links {
      gap: 1.05rem;
      flex-wrap: wrap;
      justify-content: center;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2.1rem;
    }

    .hero-content h2 {
      font-size: 3.15rem;
      margin-bottom: 1.05rem;
    }

    .hero-visual {
      height: 210px;
      order: -1;
    }

    .hero-text span {
      font-size: 1.2rem;
    }
  }
</style>
