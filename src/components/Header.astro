---
// Header component for the website
---

<header>
  <nav>
    <div class="logo">
      <img src="/logo.png" alt="kyros - Automation without the bloat" class="logo-image">
    </div>
    <ul class="nav-links">
      <li><a href="#process">Our Process</a></li>
      <li><a href="#case-studies">Case Studies</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="/demo" class="nav-demo">Demo</a></li>
      <li><a href="/portal" class="nav-login">Log In</a></li>
      <li><a href="/discovery" class="nav-cta">Book A Call</a></li>
    </ul>
  </nav>
  
  <div class="section-container">
    <div class="hero-grid">
      <div class="hero-content">
        <h2>Automation without the bloat. ROI without the gamble.</h2>
      </div>
              <div class="hero-visual">
          <div class="hero-images-container">
            <div class="hero-image-container left-angled">
              <img src="/workflow-dashboard.png?v=2" alt="kyros workflow dashboard" class="hero-image-3d">
            </div>
            <div class="hero-image-container right-angled">
              <img src="/reporting-hub.png?v=2" alt="kyros reporting hub" class="hero-image-3d">
            </div>
            <div class="center-background-dim"></div>
            <div class="hero-image-container center-overlay">
              <img src="/centre-img.png?v=2" alt="kyros center overlay" class="hero-image-3d">
            </div>
          </div>
        </div>
    </div>
  </div>
</header>

<style>
  header {
          min-height: 70vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    text-align: left;
    padding: 0;
    background: linear-gradient(135deg, #F8F9FA 0%, #ffffff 100%);
  }

  nav {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.75rem 3.15rem;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    z-index: 10;
  }

  .logo {
    display: flex;
    align-items: center;
  }

  .logo-image {
    height: 180px;
    width: auto;
    max-width: 400px;
    object-fit: contain;
    transition: transform 0.3s ease;
  }

  .logo-image:hover {
    transform: scale(1.05);
  }

  .nav-links {
    display: flex;
    list-style: none;
    gap: 2.5rem;
    margin: 0;
    padding: 0;
    align-items: center;
  }

  .nav-links a {
    text-decoration: none;
    color: #033463 !important;
    font-weight: 500;
    font-size: 1.3rem;
    transition: all 0.3s ease;
    padding: 0.5rem 0;
  }

  .nav-demo {
    color: var(--accent-blue) !important;
    padding: 0.75rem 1.5rem !important;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  .nav-demo:hover {
    color: var(--electric-blue-dark) !important;
    transform: translateY(-2px);
  }

  header .nav-login {
    color: var(--accent-blue) !important;
    border: 2px solid var(--accent-blue);
    padding: 0.75rem 1.5rem !important;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.3s ease;
  }

  header .nav-login:hover {
    background: var(--accent-blue);
    color: #FFFFFF !important;
    transform: translateY(-2px);
  }

  .nav-links a:hover {
    color: var(--accent-blue);
    transform: translateY(-2.1px); /* 5% larger: -2px to -2.1px */
  }

  header .nav-cta {
    background: linear-gradient(135deg, var(--accent-blue) 0%, var(--electric-blue-dark) 100%);
    color: #FFFFFF !important;
    padding: 0.7875rem 1.575rem !important; /* 5% larger: 0.75rem 1.5rem to 0.7875rem 1.575rem */
    border-radius: 8.4px; /* 5% larger: 8px to 8.4px */
    font-weight: 700;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.3);
    box-shadow: 0 4.2px 15.75px rgba(0, 123, 255, 0.3); /* 5% larger shadows */
    transition: all 0.3s ease;
  }

  header .nav-cta:hover {
    transform: translateY(-3.15px) !important; /* 5% larger: -3px to -3.15px */
    box-shadow: 0 6.3px 21px rgba(0, 123, 255, 0.4); /* 5% larger shadows */
    color: #FFFFFF !important;
  }

      .hero-grid {
      display: grid;
      grid-template-columns: 1.5fr 1fr;
      gap: 1rem;
      align-items: center;
      width: 100%;
      margin-top: 120px;
    }

  .hero-content {
    max-width: 630px; /* 5% larger: 600px to 630px */
    color: #033463;
  }

  .hero-content h2 {
    font-size: 4.2rem; /* 5% larger: 4rem to 4.2rem */
    font-weight: 700;
    color: #033463 !important;
    margin: 0;
    line-height: 1.2;
    text-align: left;
  }

  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
  }

      .hero-images-container {
      display: flex;
      gap: 5rem;
      align-items: center;
      justify-content: center;
      perspective: 1200px;
    }

  .hero-image-container {
    position: relative;
    width: 520px;
    height: 700px;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .right-angled {
    width: 520px;
    height: 520px;
  }

  .center-background-dim {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 450px;
    height: 600px;
    z-index: 15;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 15px;
    backdrop-filter: blur(2px);
  }

  .center-overlay {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 450px;
    height: 600px;
    z-index: 20;
    padding: 30px 20px 20px 20px;
  }

  .left-angled {
    transform: perspective(1200px) rotateY(18deg) rotateX(6deg);
    opacity: 0.6;
  }

  .right-angled {
    transform: perspective(1200px) rotateY(-18deg) rotateX(-6deg);
    opacity: 0.6;
  }



      .hero-image-3d {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }



  @media (max-width: 768px) {
    header {
      min-height: 100vh;
      padding: 1.05rem; /* 5% larger: 1rem to 1.05rem */
    }

    nav {
      padding: 1.05rem; /* 5% larger: 1rem to 1.05rem */
      flex-direction: column;
      gap: 1.05rem; /* 5% larger: 1rem to 1.05rem */
    }

    .logo-image {
      height: 120px;
    }

    .nav-links {
      gap: 1.05rem; /* 5% larger: 1rem to 1.05rem */
      flex-wrap: wrap;
      justify-content: center;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2.1rem; /* 5% larger: 2rem to 2.1rem */
    }

    .hero-content h2 {
      font-size: 3.15rem; /* 5% larger: 3rem to 3.15rem */
      margin-bottom: 1.05rem; /* 5% larger: 1rem to 1.05rem */
    }

    .hero-visual {
      height: auto;
      order: -1;
    }

    .hero-images-container {
      flex-direction: column;
      gap: 1rem;
    }

    .left-angled,
    .right-angled {
      transform: perspective(800px) rotateY(0deg) rotateX(0deg);
    }

    .hero-image-container {
      width: 480px;
      height: 650px;
    }

    .center-background-dim {
      width: 400px;
      height: 550px;
    }

    .center-overlay {
      width: 400px;
      height: 550px;
    }

    .right-angled {
      width: 480px;
      height: 480px;
    }
  }
</style>
