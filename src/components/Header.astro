---
// Header component for the website
---

<header>
  <nav>
    <div class="logo">
      <img src="/logo.png" alt="kyros - Automation without the bloat" class="logo-image">
    </div>
    <ul class="nav-links">
      <li><a href="#process">Our Process</a></li>
      <li><a href="#case-studies">Case Studies</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="/discovery" class="nav-cta">Book A Call</a></li>
    </ul>
  </nav>
  
  <div class="section-container">
    <div class="hero-grid">
      <div class="hero-content">
        <h2>Automation without the bloat. ROI without the gamble.</h2>
      </div>
      <div class="hero-visual">
        <!-- Placeholder for banner image -->
      </div>
    </div>
  </div>
</header>

<style>
  header {
    min-height: 90vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: center;
    text-align: left;
    padding: 0;
    background: linear-gradient(135deg, #F8F9FA 0%, #ffffff 100%);
  }

  nav {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 3.15rem;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    z-index: 10;
  }

  .logo {
    display: flex;
    align-items: center;
  }

  .logo-image {
    height: 180px;
    width: auto;
    max-width: 400px;
    object-fit: contain;
    transition: transform 0.3s ease;
  }

  .logo-image:hover {
    transform: scale(1.05);
  }

  .nav-links {
    display: flex;
    list-style: none;
    gap: 2.625rem; /* 5% larger: 2.5rem to 2.625rem */
    margin: 0;
    padding: 0;
    align-items: center;
  }

  .nav-links a {
    text-decoration: none;
    color: var(--text-dark);
    font-weight: 500;
    font-size: 1.155rem; /* 5% larger: 1.1rem to 1.155rem */
    transition: all 0.3s ease;
    padding: 0.525rem 0; /* 5% larger: 0.5rem to 0.525rem */
  }

  .nav-links a:hover {
    color: var(--accent-blue);
    transform: translateY(-2.1px); /* 5% larger: -2px to -2.1px */
  }

  .nav-cta {
    background: linear-gradient(135deg, var(--accent-blue) 0%, var(--electric-blue-dark) 100%);
    color: white !important;
    padding: 0.7875rem 1.575rem !important; /* 5% larger: 0.75rem 1.5rem to 0.7875rem 1.575rem */
    border-radius: 8.4px; /* 5% larger: 8px to 8.4px */
    font-weight: 600;
    box-shadow: 0 4.2px 15.75px rgba(0, 123, 255, 0.3); /* 5% larger shadows */
    transition: all 0.3s ease;
  }

  .nav-cta:hover {
    transform: translateY(-3.15px) !important; /* 5% larger: -3px to -3.15px */
    box-shadow: 0 6.3px 21px rgba(0, 123, 255, 0.4); /* 5% larger shadows */
    color: white !important;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: 4.2rem;
    align-items: center;
    width: 100%;
    margin-top: 200px;
  }

  .hero-content {
    max-width: 630px; /* 5% larger: 600px to 630px */
  }

  .hero-content h2 {
    font-size: 4.2rem; /* 5% larger: 4rem to 4.2rem */
    font-weight: 700;
    color: var(--text-dark);
    margin: 0;
    line-height: 1.2;
    text-align: left;
  }

  .hero-visual {
    height: 420px; /* 5% larger: 400px to 420px */
    background: linear-gradient(135deg, var(--accent-blue) 0%, #0056b3 100%);
    border-radius: 12.6px; /* 5% larger: 12px to 12.6px */
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.26rem; /* 5% larger: 1.2rem to 1.26rem */
    font-weight: 500;
    opacity: 0.8;
  }

  @media (max-width: 768px) {
    header {
      min-height: 100vh;
      padding: 1.05rem; /* 5% larger: 1rem to 1.05rem */
    }

    nav {
      padding: 1.05rem; /* 5% larger: 1rem to 1.05rem */
      flex-direction: column;
      gap: 1.05rem; /* 5% larger: 1rem to 1.05rem */
    }

    .logo-image {
      height: 120px;
    }

    .nav-links {
      gap: 1.05rem; /* 5% larger: 1rem to 1.05rem */
      flex-wrap: wrap;
      justify-content: center;
    }

    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2.1rem; /* 5% larger: 2rem to 2.1rem */
    }

    .hero-content h2 {
      font-size: 3.15rem; /* 5% larger: 3rem to 3.15rem */
      margin-bottom: 1.05rem; /* 5% larger: 1rem to 1.05rem */
    }

    .hero-visual {
      height: 210px; /* 5% larger: 200px to 210px */
      order: -1;
    }
  }
</style>
