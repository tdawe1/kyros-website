---
// Demo page - Coming Soon
import Layout from '../layouts/Layout.astro';
---

<Layout title="Demo - Coming Soon | kyros">
  <main class="demo-page">
    <div class="demo-container">
      <div class="demo-content">
        <div class="demo-icon">🚧</div>
        <h1>Demo Coming Soon</h1>
        <p class="demo-description">
          Our public demo environment is still under development, but we're more than happy to show you around some of our projects.
        </p>
        <div class="demo-cta">
          <p>Let us know what kind of tool you'd like to see at:</p>
          <a href="mailto:hello@kyros.solutions" class="email-link">hello@kyros.solutions</a>
        </div>
        <div class="demo-features">
          <h2>What you can expect from our demo:</h2>
          <ul>
            <li>🎯 <strong>ROI-focused automation</strong> - See how our tools deliver measurable returns</li>
            <li>⚡ <strong>Lightweight solutions</strong> - No bloat, just practical functionality</li>
            <li>🔓 <strong>Team enablement</strong> - Tools that empower your team, not create dependency</li>
            <li>📊 <strong>Real workflows</strong> - Actual use cases from our client projects</li>
          </ul>
        </div>
        <a href="/" class="back-home">← Back to Home</a>
      </div>
    </div>
  </main>
</Layout>

<style>
  .demo-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #F8F9FA 0%, #ffffff 100%);
    padding: 2rem 1rem;
  }

  .demo-container {
    max-width: 800px;
    width: 100%;
    text-align: center;
  }

  .demo-content {
    background: white;
    padding: 4rem 3rem;
    border-radius: 20px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
    position: relative;
    overflow: hidden;
  }

  .demo-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple), var(--accent-orange));
  }

  .demo-icon {
    font-size: 4rem;
    margin-bottom: 2rem;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-10px);
    }
    60% {
      transform: translateY(-5px);
    }
  }

  h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    color: var(--text-dark);
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  .demo-description {
    font-size: clamp(1.1rem, 2.5vw, 1.3rem);
    line-height: 1.6;
    color: var(--text-dark);
    margin-bottom: 3rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .demo-cta {
    background: linear-gradient(135deg, var(--accent-blue) 0%, var(--electric-blue-dark) 100%);
    padding: 2.5rem;
    border-radius: 15px;
    margin-bottom: 3rem;
    color: white;
  }

  .demo-cta p {
    font-size: 1.1rem;
    margin-bottom: 1rem;
    opacity: 0.9;
  }

  .email-link {
    display: inline-block;
    font-size: 1.3rem;
    font-weight: 600;
    color: white;
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    transition: all 0.3s ease;
    border: 2px solid rgba(255, 255, 255, 0.3);
  }

  .email-link:hover {
    background: rgba(255, 255, 255, 0.3);
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
  }

  .demo-features {
    text-align: left;
    margin-bottom: 3rem;
  }

  .demo-features h2 {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--text-dark);
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .demo-features ul {
    list-style: none;
    padding: 0;
    display: grid;
    gap: 1rem;
  }

  .demo-features li {
    font-size: 1.1rem;
    line-height: 1.5;
    color: var(--text-dark);
    padding: 1rem;
    background: #f8f9fa;
    border-radius: 8px;
    border-left: 4px solid var(--accent-blue);
  }

  .demo-features strong {
    color: var(--text-dark);
  }

  .back-home {
    display: inline-block;
    color: var(--accent-blue);
    text-decoration: none;
    font-weight: 500;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    padding: 0.5rem 0;
  }

  .back-home:hover {
    color: var(--electric-blue-dark);
    transform: translateX(-5px);
  }

  @media (max-width: 768px) {
    .demo-content {
      padding: 3rem 2rem;
    }

    .demo-cta {
      padding: 2rem 1.5rem;
    }

    .demo-features ul {
      grid-template-columns: 1fr;
    }
  }

  @media (min-width: 769px) {
    .demo-features ul {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
